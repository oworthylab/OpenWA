# @openwa/dashboard

TanStack Start dashboard deployed to Cloudflare Pages. **Status (Sprint 1):** stub only.

The existing Vite SPA in `/dashboard` (workspace root, *not* `apps/dashboard`) remains in place and will be migrated to TanStack Start during Sprint 4.
