# Archived: Legacy NestJS Dashboard E2E (Playwright)

These Playwright specs target the legacy Vite dashboard against the
legacy NestJS API. Both have been superseded by the serverless rewrite
in `apps/api` + the new dashboard in `apps/dashboard`. See
[`docs/SELF_HOST.md`](../../docs/SELF_HOST.md).
